{
  "type": "success-minimal",
  "description": "Minimal success message when even truncated version is too long",
  "template": [
    "🎉 {undername}_{ownerArnsName}.ar.io → {txId}"
  ],
  "variables": ["undername", "ownerArnsName", "txId"],
  "maxLength": 280
}
